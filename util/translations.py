from PyQt5.QtCore import  QCoreApplication

"""
    `_translate` is a keyword in PyQt5(pylupdate5) translations.
    So we keep an underline before the variable, even it is used in other packages.
"""
_translate = QCoreApplication.translate
